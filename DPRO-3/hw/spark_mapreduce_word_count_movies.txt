In the movies.csv file there is a films database. A film name is cintained in the 2nd column title.Split film names by single words and count their frequencies, then show the most frequent word.Solution:1. mv docker-compose.yml Documents/docker/2. docker-compose up3. docker cp Documents/netology/DEGP-3/DPRO-3/movies.csv ea5f09275571:/opt/bitnami/spark4. docker exec -it ea5f09275571 bash5. I have no name!@ea5f09275571:/opt/bitnami/spark$ pyspark6. df = sc.textFile(‘movies.csv’)7. titles = df.map(lambda line: line.split(',')[1].split(' '))   8. words = sc.parallelize([(word, 1) for i in titles.collect() for word in i])9. words.reduceByKey(lambda a,b: a+b).takeOrdered(5, key=lambda x: -x[1])Answer:'of' - 946